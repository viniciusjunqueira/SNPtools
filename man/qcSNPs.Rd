% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qcSNPs.R
\name{qcSNPs}
\alias{qcSNPs}
\title{Quality Control for SNPDataLong with optional criteria}
\usage{
qcSNPs(x, ...)
}
\arguments{
\item{x}{An object of class SNPDataLong.}

\item{missing_ind}{Maximum allowed proportion of missing data per individual (optional). *[Currently not implemented]*}

\item{missing_snp}{Maximum allowed proportion of missing data per SNP (optional). *[Currently not implemented]*}

\item{min_snp_cr}{Minimum acceptable call rate for SNPs (e.g., 0.95).}

\item{min_maf}{Minimum minor allele frequency allowed for SNPs (e.g., 0.05).}

\item{hwe}{p-value threshold for Hardy-Weinberg equilibrium test (e.g., 1e-6).}

\item{snp_position}{Logical. If TRUE, removes SNPs mapped to the same position, keeping the one with highest MAF.}

\item{no_position}{Logical. If TRUE, removes SNPs without defined map positions.}

\item{snp_mono}{Logical. If TRUE, removes monomorphic SNPs.}

\item{remove_chr}{Character vector of chromosomes to exclude (e.g., c("X", "Y")).}

\item{action}{One of "report" (list of removed SNPs), "filter" (returns filtered SNPDataLong), or "both" (both results).}
}
\value{
Depending on the action argument:
- "report": list of SNPs removed by each filter and SNPs retained.
- "filter": filtered SNPDataLong object.
- "both": list containing the filtered object and detailed report.
}
\description{
Applies flexible quality control filters on an object of class \code{SNPDataLong}. 
Supports call rate filtering, minor allele frequency (MAF), Hardy-Weinberg equilibrium (HWE),
removal of monomorphic SNPs, exclusion of specific chromosomes, optionally removing SNPs without positions,
and optionally removing SNPs at the same genomic position.
}
\examples{
\dontrun{
set.seed(123)
mat <- matrix(sample(c(0, 1, 2, NA), 100, replace = TRUE, prob = c(0.4, 0.4, 0.15, 0.05)),
              nrow = 10, ncol = 10)
colnames(mat) <- paste0("snp", 1:10)
rownames(mat) <- paste0("ind", 1:10)
map <- data.frame(Name = colnames(mat), Chromosome = 1, Position = 1:10)
x <- new("SNPDataLong", geno = mat, map = map)

# Example with multiple filters and returning a detailed report
qcSNPs(x, min_snp_cr = 0.8, min_maf = 0.05, snp_mono = TRUE, no_position = TRUE, snp_position = TRUE, action = "report")
}

}
